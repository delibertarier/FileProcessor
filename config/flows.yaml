# Main flow configuration file for FileProcessor.
#
# Folder layout (see data/README.md):
# - Outbound: internal → external (CSV in → XML out). Use data/outbound/*.
# - Inbound:  external → internal (XML in → CSV out). Use data/inbound/*.
#
# Each flow ties together: input/success/error/archive dirs, mapping XLS,
# optional XSD/skeleton (for csv_to_xml), filters and grouping.
#
# Windows: use absolute paths like E:\FTP\... — not ".E:\..." (leading dot breaks paths).

flows:
  # --- Outbound: CSV from internal systems → XML for external systems ---
  - name: rolls_outbound
    mode: csv_to_xml

    input_dir: ./data/outbound/in
    success_dir: ./data/outbound/success
    error_dir: ./data/outbound/error
    archive_dir: ./data/outbound/archive
    in_progress_dir: ./data/outbound/in_progress

    file_glob: "TRA*.TXT"

    input_format: csv
    delimiter: "|"

    mapping_file: ./examples/mapping ROLLS SSW outgoing.xlsx
    mapping_sheet_name: "CSV to XML"
    skeleton_xml: ./examples/skeleton_PldaSswDeclaration.xml
    xsd_file: ./examples/PLDASSW.xsd

    root_element_name: PldaSswDeclaration

    # Example filters (disabled):
    # filters:
    #   - column_index: 5
    #     operator: eq
    #     value: "ACTIVE"
    #
    # Example grouping (disabled):
    # grouping:
    #   group_by_column_index: 1
    #   group_root_xpath: /PldaSswDeclaration/MessageBody/SADDossier
    #   item_xpath: /PldaSswDeclaration/MessageBody/SADDossier/GoodsDeclaration

  # --- Inbound: XML from external systems → CSV for internal systems ---
  - name: ssw_inbound
    mode: xml_to_csv

    input_dir: ./data/inbound/in
    success_dir: ./data/inbound/success
    error_dir: ./data/inbound/error
    archive_dir: ./data/inbound/archive
    in_progress_dir: ./data/inbound/in_progress

    file_glob: "TE_FELUY*.xml" #file format is BU_typeofmessage

    input_format: csv
    delimiter: "|"

    mapping_file: ./examples/mapping ROLLS SSW outgoing.xlsx
    mapping_sheet_name: "SSW return message"
    skeleton_xml: null
    xsd_file: null

    root_element_name: PldaSswDeclaration

  - name: emcs_arc_all
    mode: xml_to_csv

    input_dir: ./data/inbound/in
    success_dir: ./data/inbound/success
    error_dir: ./data/inbound/error
    archive_dir: ./data/inbound/archive
    in_progress_dir: ./data/inbound/in_progress

    file_glob: "ARC_ALL*.xml" #files containing IE801 messages

    input_format: csv
    delimiter: "|"

    mapping_file: ./examples/mapping ROLLS SSW outgoing.xlsx
    mapping_sheet_name: "ARC response 801"
    skeleton_xml: null
    xsd_file: null

    root_element_name: EmcsSswDeclaration

  - name: emcs_arr_not
    mode: xml_to_csv

    input_dir: ./data/inbound/in
    success_dir: ./data/inbound/success
    error_dir: ./data/inbound/error
    archive_dir: ./data/inbound/archive
    in_progress_dir: ./data/inbound/in_progress

    file_glob: "ARR_NOT*.xml" #files containing IE801 messages

    input_format: csv
    delimiter: "|"

    mapping_file: ./examples/mapping ROLLS SSW outgoing.xlsx
    mapping_sheet_name: "Binnenkomende Arrival EAD 801"
    skeleton_xml: null
    xsd_file: null

    root_element_name: EmcsSswDeclaration

    # One CSV per BodyEadEsad line; filename suffix from BodyRecordUniqueReference (e.g. ..._1.csv)
    xml_line_xpath: BodyEadEsad
    xml_line_output: file_per_line
    xml_line_filename_element: BodyRecordUniqueReference

  - name: emcs_dec_rej
    mode: xml_to_csv

    input_dir: ./data/inbound/in
    success_dir: ./data/inbound/success
    error_dir: ./data/inbound/error
    archive_dir: ./data/inbound/archive
    in_progress_dir: ./data/inbound/in_progress

    file_glob: "DEC_REJ*.xml" #files containing IE801 messages

    input_format: csv
    delimiter: "|"

    mapping_file: ./examples/mapping ROLLS SSW outgoing.xlsx
    mapping_sheet_name: "IE704 functional error"
    skeleton_xml: null
    xsd_file: null

    root_element_name: EmcsSswDeclaration

  - name: emcs_cmd_err
    mode: xml_to_csv

    input_dir: ./data/inbound/in
    success_dir: ./data/inbound/success
    error_dir: ./data/inbound/error
    archive_dir: ./data/inbound/archive
    in_progress_dir: ./data/inbound/in_progress

    file_glob: "CMD_ERR*.xml" #files containing IE801 messages

    input_format: csv
    delimiter: "|"

    mapping_file: ./examples/mapping ROLLS SSW outgoing.xlsx
    mapping_sheet_name: "IE917 xml error"
    skeleton_xml: null
    xsd_file: null

    root_element_name: EmcsSswDeclaration

  - name: emcs_dec_dat
    mode: xml_to_csv

    input_dir: ./data/inbound/in
    success_dir: ./data/inbound/success
    error_dir: ./data/inbound/error
    archive_dir: ./data/inbound/archive
    in_progress_dir: ./data/inbound/in_progress

    file_glob: "DEC_DAT*.xml" #files containing IE801 messages

    input_format: csv
    delimiter: "|"

    mapping_file: ./examples/mapping ROLLS SSW outgoing.xlsx
    mapping_sheet_name: "IE815 confirmation"
    skeleton_xml: null
    xsd_file: null

    root_element_name: EmcsSswDeclaration

  - name: emcs_can_dec
    mode: xml_to_csv

    input_dir: ./data/inbound/in
    success_dir: ./data/inbound/success
    error_dir: ./data/inbound/error
    archive_dir: ./data/inbound/archive
    in_progress_dir: ./data/inbound/in_progress

    file_glob: "CAN_DEC*.xml" #files containing IE801 messages

    input_format: csv
    delimiter: "|"

    mapping_file: ./examples/mapping ROLLS SSW outgoing.xlsx
    mapping_sheet_name: "IE810 Cancel"
    skeleton_xml: null
    xsd_file: null

    root_element_name: EmcsSswDeclaration

  - name: emcs_emg_prt
    mode: xml_to_csv

    input_dir: ./data/inbound/in
    success_dir: ./data/inbound/success
    error_dir: ./data/inbound/error
    archive_dir: ./data/inbound/archive
    in_progress_dir: ./data/inbound/in_progress

    file_glob: "EMG_PRT*.xml" #files containing IE801 messages

    input_format: csv
    delimiter: "|"

    mapping_file: ./examples/mapping ROLLS SSW outgoing.xlsx
    mapping_sheet_name: "IE815 deferred"
    skeleton_xml: null
    xsd_file: null

    root_element_name: EmcsSswDeclaration
