Example data for FileProcessor.

- `sample_orders.csv`: example CSV input with multiple orders and lines.
- `sample_orders.xml`: example XML output grouped by order.
- `orders.xsd`: simple XSD used to validate the example XML.

